
# VK ⇄ Telegram бот: карточка товара + два отложенных поста

Бот берёт сообщение в Telegram (фото + текст) и:
1) создаёт **карточку товара ВК (Market)**;
2) делает **два отложенных поста на завтра** (по умолчанию `14:00` и `18:00`).

Описание товара формируется так: из сообщения берётся весь свободный текст (без строк `Цена/Размеры/Артикул`) и дополняется этими полями внизу.

## Установка
1. Python 3.10+.
2. Установить зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Создать `.env` на основе `.env.example` и заполнить:
   - `BOT_TOKEN` — токен бота у @BotFather
   - `ADMIN_ID` — ваш Telegram ID
   - `VK_GROUP_ID` — ID сообщества ВК (без минуса)
   - `VK_TOKEN` — ключ сообщества с правами: `market, photos, wall, groups, offline`
   - `VK_CATEGORY_ID` — категория товара
   - `TIMEZONE` — часовой пояс (например `Europe/Moscow`)
   - `POST_TIMES` — слоты на завтра (по умолчанию `14:00,18:00`)

### Подготовка сообщества ВК
- Включите раздел **Товары** и задайте валюту (Настройки → Разделы).
- Получите токен с нужными правами (Настройки → Работа с API).
- `VK_CATEGORY_ID` найдите через `market.getCategories` в тестовом стенде VK API.

## Запуск
```bash
python main.py
```

## Использование
1. Отправьте боту одно сообщение с фото и текстом, например:
   ```
   MA-1 classic bomber jacket
   Цена: 5500 ₽
   Размеры: S/M/L/XL
   Артикул: MA1-BLK

   Состояние: новое
   ```
2. Нажмите:
   - **✅ Создать товар ВК** — загрузит фото, создаст товар и пришлёт ссылку.
   - **🕒 Отложить посты ВК** — запланирует два поста на завтра на время из `POST_TIMES`.

## Частые ошибки
- `market is disabled` — включите «Товары» и валюту.
- `Access denied` — у токена нет прав. Создайте заново с правами выше.
- `category_id invalid` — проверьте `VK_CATEGORY_ID`.
